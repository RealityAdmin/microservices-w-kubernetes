apiVersion: v1
kind: Service
metadata:
  name: orders-service
  labels:
    run: orders
spec:
  type: NodePort
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
  selector:
    app: orders